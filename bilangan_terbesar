def bilangan_terbesar():
    a = float(input("masukkan angka pertama"))
    b = float(input("masukkan angka kedua"))
    
    if a>b:
        print("angkaterbesar = {a}")
    elif b>a:
        print("angkaterbesar = {b}")
    else:
        print ("kedua angka sama besar")


bilangan_terbesar
